using UnityEngine;

public class Note : MonoBehaviour
{
    public Sprite noteSprite;  // Image that represents the note

    // You can expand this to include more properties, such as note text, sound, etc.
    // public string noteText;  // If you want to show text instead of or alongside the image
}